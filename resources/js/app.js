import "./bootstrap";
import Alpine from "alpinejs";
import Timer from "easytimer.js";

// Make ALpine global instance
window.Alpine = Alpine;
Alpine.start();

// Make Easy Timer global Instance
window.Timer = Timer;
